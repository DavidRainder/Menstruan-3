using UnityEngine;

public class TEST_CameraMovementStart : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        GameManager.MoveCamera(0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
